# 개발 로그 - 2025-11-05 11:57

## 작업 요약
`.claude/CLAUDE.md` 파일 생성 및 프로젝트 가이드라인 수립

## 주요 변경사항

### 1. `.claude/` 폴더 구조 생성
- `.claude/` 디렉토리 생성
- MCP 및 Claude Code 관련 설정을 총체적으로 관리하기 위한 폴더 구조 확립

### 2. `.claude/CLAUDE.md` 파일 작성
프로젝트의 핵심 정보를 담은 가이드 문서 작성:

**포함된 주요 섹션:**
- **Project Overview**: 문화유산 오디오 가이드 생성 파이프라인 설명
  - 3단계 파이프라인: 정보 검색 → 스크립트 생성 → 오디오 합성

- **Environment Setup**:
  - pyenv 기반 Python 버전 관리 (3.12.12)
  - `.venv` 가상환경 사용
  - `setup-venv.sh` 스크립트를 통한 자동 설정

- **Architecture**:
  - Pipeline 1 (info_retrieval.py): 키워드 → 마크다운 정보
  - Pipeline 2 (script_gen.py): 정보 → 오디오 가이드 스크립트
  - Pipeline 3 (audio_gen.py): 스크립트 → MP3 파일

- **Working with Claude Code**:
  - 한국어로 주석 및 문서 작성
  - Python 명령 실행 전 venv 활성화 확인 필수
  - `/compact` 실행 후 dev-log 자동 생성
  - `dev-log/`: 타임스탬프 기반 개발 로그
  - `docs/`: 기획/설계 문서

### 3. 주요 의존성 확인
- `openai>=0.27.0` - LLM 및 TTS API
- `python-dotenv>=1.0.0` - 환경변수 관리
- `langchain-openai>=0.1.0` - 프롬프트/체인 구조
- `exa-py>=1.0.0` - 웹 검색 (현재 주석 처리)

## 설계 결정사항

1. **파이프라인 독립성**: 각 단계를 파일 I/O로 연결하여 독립적으로 실행 가능하도록 설계
2. **MVP 우선**: v0.1에서는 전체 workflow 검증에 집중
3. **확장성 고려**: 각 파이프라인 단계별 extension point 명시
4. **한국어 중심**: 문화유산 도메인 특성상 한국어 주석/문서 작성

## 다음 단계

### 즉시 필요한 작업:
- 실제 파이프라인 구현 (`src/pipelines/*.py`)
- `.env` 파일 설정 (OPENAI_API_KEY)
- `src/main.py` 오케스트레이션 로직 구현

### v0.2 이후 계획:
- 검색 소스 확장 (Exa.ai → Wikipedia → museum.go.kr API)
- 톤 변화 (감성/교육/아동용)
- BGM 자동 믹싱 (Suno 통합)
- 메타데이터 JSON 추적
- UI 레이어 (Streamlit/Next.js)

## 참고 문서
- `docs/plan.md` - 상세 설계 문서
- `setup-venv.sh` - 환경 설정 스크립트
- `.python-version` - Python 3.12.12

## 비고
- 현재 `src/pipelines/` 디렉토리는 `.gitkeep` 파일만 존재
- `outputs/` 하위 폴더(info/, script/, audio/)는 준비됨
- README.md 없음 (CLAUDE.md가 대체)
