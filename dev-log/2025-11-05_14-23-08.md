# Review Log - 2025-11-05 14:23:08

- 브랜치/커밋: main @ e6f7db3f8878d6c6b8b795a45cccb5a6d259e0c8 (merge of jacti/workflow-02_inforetrieval into jacti/main-reviewer)
- 변경 개요: 스크립트 생성 파이프라인 구현, 프롬프트 템플릿 및 로더 추가, dry-run 샘플 출력 포함
- 주요 관찰: Pipeline 2가 파일명에서 공백을 언더스코어로 치환하여 info 파이프라인 산출물과 경로 계약이 불일치
- 발견 이슈:
  1. `src/pipelines/script_gen.py:121` – info 파일 경로를 `keyword.replace(" ", "_")`로 구성하여 Pipeline 1이 생성하는 `outputs/info/[keyword].md` (공백 유지)와 호환되지 않음. 실환경에서 dry_run이 아니면 FileNotFoundError 발생. → `_sanitize_filename`과 동일한 규칙을 공유하거나 slugifying 전략을 문서/타 파이프라인과 동기화 필요.
- 후속 액션 제안: script_gen 파이프라인에서 파일명 처리 로직을 Pipeline 1과 동일하게 맞추고, 향후 재사용을 위해 공통 유틸로 추출하는 방안 검토.
- 파이프라인별 피드백 요약:
  - 정보 검색: 현재 산출물 구조를 유지하고 있으나, Script 파이프라인이 다른 파일명 규칙을 사용해 통합 시 실패 가능 → 조정 필요.
  - 스크립트 작성: slug 처리 로직 수정 전까지 실제 실행 시 오류.
  - 오디오 생성: 상위 단계 실패로 인해 입력 파일을 받지 못하는 리스크 공유.
