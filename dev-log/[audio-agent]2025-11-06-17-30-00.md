# 오디오 생성 파이프라인 버그 수정: OpenAI TTS API 사용법 오류

**일시:** 2025-11-06 17:30:00
**담당:** Claude Code (Audio Generation Agent)
**작업 범위:** 실제 오디오 생성 실패 원인 분석 및 수정 계획 수립

---

## 📝 세션 요약

### 세션 시작 배경
- `/compact` 명령 실행으로 이전 컨텍스트 압축
- 이전 작업: 파이프라인 통합 완료 (path_sanitizer 적용)
- 모든 dry_run 테스트는 성공했으나, **실제 API 호출 시 실패** 발생

### 사용자 요청
> "현재 dry-run 말고 실제로 오디오 생성을 시도하면 실패가 떠. 이유를 분석해봐. 특히 웹에 검색해보고 open ai tts 를 지금 방식처럼 사용하는 것이 맞는지 재 검증해봐"

---

## 🔍 문제 진단

### 1. 웹 검색을 통한 검증

**검색 쿼리 3개:**
1. "OpenAI TTS API speech create python 2025 usage example"
2. "openai python sdk audio speech create response content 2025"
3. "openai tts gpt-4o-mini-tts model name error python"

**검증 결과:**
- ✅ 모델명 `gpt-4o-mini-tts` 유효 (다른 옵션: `tts-1`, `tts-1-hd`)
- ✅ Voice 옵션 유효 (`alloy`, `echo`, `fable`, `onyx`, `nova`, `shimmer`)
- ❌ **응답 처리 방식이 공식 문서와 다름** → 핵심 문제 발견!

### 2. 발견한 핵심 버그

**문제 파일:** `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/src/pipelines/audio_gen.py`

**잘못된 코드 (125줄):**
```python
def _generate_audio_openai(...) -> bytes:
    # ...
    response = client.audio.speech.create(
        model=model,
        voice=voice,
        input=text,
        speed=speed
    )

    # ❌ 문제: response.content 속성이 존재하지 않거나 올바른 방법이 아님
    audio_data = response.content

    logger.info(f"음성 생성 완료 ({len(audio_data)} bytes)")
    return audio_data
```

**공식 문서의 올바른 방법:**
```python
# 방법 1: stream_to_file() 직접 사용 (권장)
response = client.audio.speech.create(...)
response.stream_to_file("output.mp3")

# 방법 2: with_streaming_response 사용
with client.audio.speech.with_streaming_response.create(...) as response:
    response.stream_to_file("output.mp3")
```

**근거 자료:**
- OpenAI 공식 문서: https://platform.openai.com/docs/guides/text-to-speech
- DataCamp 튜토리얼 (2025년 3월): https://www.datacamp.com/tutorial/how-to-use-the-openai-text-to-speech-api
- GitHub 공식 SDK 소스: https://github.com/openai/openai-python/blob/main/src/openai/resources/audio/speech.py
  - 응답 타입: `HttpxBinaryResponseContent`
  - `.stream_to_file()` 메서드 제공

---

## 🛠️ 수정 계획

### 수정 대상 파일
**파일:** `src/pipelines/audio_gen.py`
**절대 경로:** `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/src/pipelines/audio_gen.py`

### 변경 사항 1: `_generate_audio_openai` 함수 시그니처 변경

**현재 (66-140줄):**
```python
def _generate_audio_openai(
    text: str,
    model: str = "gpt-4o-mini-tts",
    voice: str = "alloy",
    speed: float = 1.0,
    max_retries: int = 3,
    retry_delay: float = 2.0
) -> bytes:  # ❌ 반환 타입 변경 필요
```

**수정 후:**
```python
def _generate_audio_openai(
    text: str,
    output_path: Path,  # ✅ 출력 경로를 인자로 받음
    model: str = "gpt-4o-mini-tts",
    voice: str = "alloy",
    speed: float = 1.0,
    max_retries: int = 3,
    retry_delay: float = 2.0
) -> None:  # ✅ 파일에 직접 쓰므로 반환 값 없음
```

### 변경 사항 2: API 호출 및 파일 저장 로직

**현재 (109-139줄):**
```python
for attempt in range(1, max_retries + 1):
    try:
        logger.info(...)

        response = client.audio.speech.create(
            model=model,
            voice=voice,
            input=text,
            speed=speed
        )

        # ❌ 잘못된 부분
        audio_data = response.content

        logger.info(f"음성 생성 완료 ({len(audio_data)} bytes)")
        return audio_data  # ❌

    except Exception as e:
        # 재시도 로직...
```

**수정 후:**
```python
for attempt in range(1, max_retries + 1):
    try:
        logger.info(...)

        response = client.audio.speech.create(
            model=model,
            voice=voice,
            input=text,
            speed=speed
        )

        # ✅ 올바른 방법: stream_to_file() 사용
        response.stream_to_file(str(output_path))

        logger.info(f"음성 생성 완료: {output_path}")
        return  # ✅ 반환 값 없음

    except Exception as e:
        # 재시도 로직...
```

### 변경 사항 3: `run` 함수 호출 부분 수정

**현재 (233-244줄):**
```python
else:
    # 실제 TTS 생성
    audio_data = _generate_audio_openai(  # ❌
        text=script_text,
        model=model,
        voice=voice,
        speed=speed,
        max_retries=max_retries,
        retry_delay=retry_delay
    )

    # 파일 저장
    with open(output_path, "wb") as f:  # ❌ 중복 저장 로직
        f.write(audio_data)

    logger.info(f"✅ MP3 파일 저장 완료: {output_path.absolute()}")
```

**수정 후:**
```python
else:
    # 실제 TTS 생성 (파일에 직접 저장됨)
    _generate_audio_openai(  # ✅
        text=script_text,
        output_path=output_path,  # ✅ 경로 전달
        model=model,
        voice=voice,
        speed=speed,
        max_retries=max_retries,
        retry_delay=retry_delay
    )

    logger.info(f"✅ MP3 파일 저장 완료: {output_path.absolute()}")
```

### 변경 사항 4: Docstring 업데이트

**함수 설명 업데이트:**
```python
def _generate_audio_openai(...):
    """
    OpenAI TTS API를 호출하여 음성 파일을 생성합니다.

    생성된 오디오는 output_path에 직접 저장됩니다.

    Args:
        text: 변환할 텍스트
        output_path: 출력 파일 경로 (MP3)
        model: 사용할 TTS 모델명 (기본값: "gpt-4o-mini-tts")
        voice: 음성 종류 (alloy, echo, fable, onyx, nova, shimmer 중 선택)
        speed: 말하기 속도 (0.25 ~ 4.0, 기본값: 1.0)
        max_retries: 최대 재시도 횟수
        retry_delay: 재시도 대기 시간 (초)

    Returns:
        None (파일에 직접 저장)

    Raises:
        ValueError: API 키가 설정되지 않았거나 파라미터가 유효하지 않을 경우
        Exception: API 호출 실패 시
    """
```

---

## ⚠️ 추가 발견 사항

### `gpt-4o-mini-tts` 모델의 알려진 이슈

웹 검색 중 OpenAI 커뮤니티 포럼에서 발견:

1. **긴 오디오 불안정성**
   - 출처: https://community.openai.com/t/gpt-4o-mini-tts-produces-unusable-results/1228541
   - 1.5-2분 이상의 오디오에서 불안정
   - 권장: 긴 스크립트는 `tts-1-hd` 사용

2. **speed 파라미터 무시**
   - 출처: https://community.openai.com/t/new-tts-model-gpt-4o-mini-tts-ignoring-speed-parameter/1154883
   - `gpt-4o-mini-tts`에서 speed 파라미터가 제대로 작동하지 않는 경우 보고됨
   - `tts-1` 모델은 정상 작동

### 권장 사항

**현재 상황:**
- 스크립트 길이: 대부분 1분 이내 (MVP 기준)
- 기본 모델: `gpt-4o-mini-tts`

**제안:**
1. 기본 모델을 `tts-1`로 변경 (안정성 향상)
2. 또는 docstring에 긴 스크립트 시 주의사항 명시
3. 추후 스크립트 길이에 따라 모델 자동 선택 로직 추가

---

## 🧪 테스트 계획

### 1. dry_run 모드 확인 (기존 기능 유지)
```bash
cd /Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0
export PYTHONPATH=.
.venv/bin/python src/pipelines/audio_gen.py --keyword "테스트" --dry-run
```

**예상 결과:**
- ✅ 더미 MP3 파일 생성
- ✅ 로그 정상 출력

### 2. 실제 TTS 생성 테스트 (짧은 스크립트)
```bash
export PYTHONPATH=.
.venv/bin/python src/pipelines/audio_gen.py --keyword "테스트"
```

**테스트 스크립트:**
- 파일: `outputs/script/테스트_script.md`
- 내용: 간단한 1-2문장 (30초 이내)

**예상 결과:**
- ✅ OpenAI API 호출 성공
- ✅ `outputs/audio/테스트.mp3` 생성
- ✅ 파일 재생 가능

### 3. 실제 TTS 생성 테스트 (긴 스크립트)
```bash
export PYTHONPATH=.
.venv/bin/python src/pipelines/audio_gen.py --keyword "청자 상감운학문 매병"
```

**테스트 스크립트:**
- 파일: `outputs/script/청자 상감운학문 매병_script.md`
- 절대 경로: `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/outputs/script/청자 상감운학문 매병_script.md`
- 내용: 31줄 (약 1분 분량)

**예상 결과:**
- ✅ OpenAI API 호출 성공
- ✅ `outputs/audio/청자 상감운학문 매병.mp3` 생성
- ✅ 음질 및 재생 상태 확인

### 4. 다양한 Voice 옵션 테스트
```bash
# Nova 보이스 테스트
.venv/bin/python src/pipelines/audio_gen.py --keyword "테스트" --voice nova

# Echo 보이스 테스트
.venv/bin/python src/pipelines/audio_gen.py --keyword "테스트" --voice echo
```

---

## 🚨 MCP 서버 이슈

### 발견된 문제
- 현재 worktree 세션에서 **context-7 MCP 서버가 로드되지 않음**
- 다른 터미널 세션에서는 정상 작동
- 이유: MCP는 Claude Code 세션 시작 시에만 초기화되며 런타임 추가 불가

### 영향
- OpenAI 라이브러리 최신 문법 참조 불가
- 할루시네이션 가능성 증가
- 정확한 코드 작성 어려움

### 해결 방법
**새로운 세션 시작 필요:**

```bash
# 메인 프로젝트 디렉토리로 이동
cd /Users/jacti/develop/script_gen_v2

# Claude Code 시작 (context-7 MCP 로드)
claude code

# /mcp 명령으로 확인
/mcp
# 예상 출력: context-7 서버 확인

# 작업 디렉토리로 이동하거나 절대 경로 사용
cd /Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0
```

---

## 📁 관련 파일 경로 정리

### 수정 대상
- **메인 파일**: `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/src/pipelines/audio_gen.py`
- **수정 범위**: 66-140줄, 233-244줄

### 테스트 파일
- **짧은 스크립트**: `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/outputs/script/테스트_script.md`
- **긴 스크립트**: `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/outputs/script/청자 상감운학문 매병_script.md`

### 출력 디렉토리
- **오디오 출력**: `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/outputs/audio/`

### 이전 로그
- **통합 로그**: `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/dev-log/[audio-agent]2025-11-05-16-05-50.md`
- **구현 로그**: `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0/dev-log/[audio-agent]2025-11-05-14-16-08.md`

### 프로젝트 루트
- **메인 프로젝트**: `/Users/jacti/develop/script_gen_v2`
- **현재 worktree**: `/Users/jacti/.claude-squad/worktrees/workflow-03_audiogeneration_187502b0eeed83a0`

### Git 정보
```
브랜치: jacti/workflow-03_audiogeneration
커밋: 3084e0d
베이스: main (542a37e)
```

---

## ✅ 다음 세션 체크리스트

### 세션 시작 전
- [ ] 메인 프로젝트 디렉토리에서 Claude Code 시작
- [ ] `/mcp` 명령으로 context-7 로드 확인
- [ ] 이 로그 파일 읽기: `dev-log/[audio-agent]2025-11-06-17-30-00.md`

### 코드 수정
- [ ] `src/pipelines/audio_gen.py` 수정
  - [ ] `_generate_audio_openai` 함수 시그니처 변경
  - [ ] `response.content` → `response.stream_to_file()` 변경
  - [ ] `run` 함수 호출 부분 수정
  - [ ] Docstring 업데이트
- [ ] (선택) 기본 모델 `tts-1`로 변경

### 테스트
- [ ] dry_run 모드 테스트
- [ ] 실제 TTS 생성 테스트 (짧은 스크립트)
- [ ] 실제 TTS 생성 테스트 (긴 스크립트)
- [ ] 생성된 MP3 파일 재생 확인

### 문서화
- [ ] 새 로그 파일 생성: `[audio-agent]2025-11-06-[완료시각].md`
- [ ] 수정 내역 및 테스트 결과 문서화
- [ ] 통합 테스트 가능 여부 확인

---

## 📊 참고 자료

### 웹 검색 쿼리 결과

**쿼리 1:** "OpenAI TTS API speech create python 2025 usage example"
- DataCamp 튜토리얼: https://www.datacamp.com/tutorial/how-to-use-the-openai-text-to-speech-api
- 공식 문서: https://platform.openai.com/docs/guides/text-to-speech

**쿼리 2:** "openai python sdk audio speech create response content 2025"
- SDK 소스 코드: https://github.com/openai/openai-python/blob/main/src/openai/resources/audio/speech.py
- Azure OpenAI 가이드: https://learn.microsoft.com/en-us/azure/ai-foundry/openai/audio-completions-quickstart

**쿼리 3:** "openai tts gpt-4o-mini-tts model name error python"
- 공식 모델 문서: https://platform.openai.com/docs/models/gpt-4o-mini-tts
- 커뮤니티 이슈: https://community.openai.com/t/gpt-4o-mini-tts-produces-unusable-results/1228541

### OpenAI TTS 모델 비교

| 모델 | 품질 | 속도 | 안정성 | 긴 오디오 | speed 파라미터 |
|------|------|------|--------|-----------|----------------|
| `tts-1` | 표준 | 빠름 | 높음 | ✅ | ✅ |
| `tts-1-hd` | 고품질 | 느림 | 높음 | ✅ | ✅ |
| `gpt-4o-mini-tts` | 커스터마이징 | 빠름 | 중간 | ⚠️ | ⚠️ |

---

**작업 완료 시각:** 2025-11-06 17:30:00
**상태:** 🔍 분석 완료 (수정 대기)
**다음 작업:** context-7 MCP 로드 후 코드 수정 및 테스트

---

## 🎯 핵심 요약

**발견된 버그:**
```python
# ❌ 잘못된 코드
audio_data = response.content
```

**올바른 코드:**
```python
# ✅ 올바른 코드
response.stream_to_file(str(output_path))
```

**새로운 세션에서 할 일:**
1. context-7 MCP 로드
2. `src/pipelines/audio_gen.py` 수정
3. 테스트 실행
4. 결과 문서화
